<!DOCTYPE html>
<html lang="fr">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">

        <title>Inscription | Miftah Arabi</title>

        {# SEO #}
        <meta name="robots" content="index, follow">
        <meta name="description" content="Inscription sur Miftah Arabi, la plateforme pour apprendre l’arabe de manière simple, progressive et accessible.">
        <meta name="keywords" content="apprendre l’arabe, arabe littéraire, cours d’arabe, plateforme arabe, Miftah Arabi, débutant, francophone">
        <meta name="author" content="Zhaki El Ouarroudi">
        <meta name="publisher" content="Miftah Arabi">

        {# Couleur navigateur (mobile) #}
        <meta name="theme-color" content="#C9A227">

        {# Favicon #}

        <link rel="icon" type="image/png" href="/favicon-96x96.png" sizes="96x96">
        <link rel="icon" type="image/svg+xml" href="/favicon.svg">
        <link rel="shortcut icon" href="/favicon.ico">
        <link rel="apple-touch-icon" sizes="180x180" href="/apple-touch-icon.png">
        <meta name="apple-mobile-web-app-title" content="MyWebSite">
        <link rel="manifest" href="/site.webmanifest">

        {# Google Font #}

        <link rel="preconnect" href="https://fonts.googleapis.com">
        <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
        <link href="https://fonts.googleapis.com/css2?family=Inter:ital,opsz,wght@0,14..32,100..900;1,14..32,100..900&family=Poppins:ital,wght@0,100;0,200;0,300;0,400;0,500;0,600;0,700;0,800;0,900;1,100;1,200;1,300;1,400;1,500;1,600;1,700;1,800;1,900&display=swap" rel="stylesheet">

        {# WebpakEncore stylesheets #}
        {% block stylesheets %}
            {{ encore_entry_link_tags('app') }}
        {% endblock %}

        {# WebpakEncore javascripts #}
        {% block javascripts %}
            {{ encore_entry_script_tags('app') }}
        {% endblock %}
    </head>
    <body class='ma-auth-bg'>
        <main>
            <div class="container">
                <div class="row">
                    <div class="col-lg-6 d-none d-lg-block">
                        <img class= "img-fluid" src="/images/statics/inscription.png" alt="Une image de Miftah Arabi vous invitant à vous inscrire">
                    </div>
                    <div class="col-lg-6 d-flex flex-column gap-3 justify-content-center align-items-center">
                        <h1 class="text-center my-3">Inscription</h1>

                        {% for flash_error in app.flashes('verify_email_error') %}
                            <div class="alert alert-danger text-center" role="alert">{{ flash_error }}</div>
                        {% endfor %}

                        {{ form_start(registrationForm) }}
                            <div class="row">
                                <div class="col-lg-6 mb-3">
                                    <label for="registration_form_firstName">Prénom</label>
                                    <div class="text-danger">{{form_errors(registrationForm.firstName)}}</div>
                                    {{form_widget(registrationForm.firstName, {'attr':
                                    {'class':'form-control', 'autofocus':'autofocus'}})}}
                                </div>
                                <div class="col-lg-6 mb-3">
                                    <label for="registration_form_lastName">Nom</label>
                                    <div class="text-danger">{{form_errors(registrationForm.lastName)}}</div>
                                    {{form_widget(registrationForm.lastName, {'attr':
                                    {'class':'form-control'}})}}
                                </div>
                            </div>
                            <div class="mb-3">
                                    <label for="registration_form_email">Email</label>
                                    <div class="text-danger">{{form_errors(registrationForm.email)}}</div>
                                    {{form_widget(registrationForm.email, {'attr':
                                    {'class':'form-control'}})}}
                            </div>
                            <div class="mb-3">
                                    <label for="registration_form_password_first">Mot de passe</label>
                                    <div class="text-danger">{{form_errors(registrationForm.password.first)}}</div>
                                    {{form_widget(registrationForm.password.first, {'attr':
                                    {'class':'form-control', 'placeholder': 'Au moins 12 caractères'}})}}
                                    <small><i class="fa-solid fa-circle-exclamation"></i> Au moins une lettre minuscule et majuscule, un chiffre et un caractère spécial.</small>
                            </div>
                            <div class="mb-3">
                                    <label for="registration_form_password_second">Confirmation du mot de passe</label>
                                    <div class="text-danger">{{form_errors(registrationForm.password.second)}}</div>
                                    {{form_widget(registrationForm.password.second, {'attr':
                                    {'class':'form-control'}})}}
                            </div>
                             <div class="mb-3">
                             <div class="text-danger">{{form_errors(registrationForm.agreeTerms)}}</div>
                                {{form_widget(registrationForm.agreeTerms)}}
                                <label for="registration_form_agreeTerms">Lire et accepter <a href="{{path('app_visitor_terms_of_service')}}" target="_blank">les conditions générales d'utilisation</a></label>
                            </div>
                            <div>
                                <input type="submit" formnovalidate class= "btn btn-primary w-100 shadow" value="S'inscrire">
                            </div>
                        {{ form_end(registrationForm) }}

                        

                        <div class="text-center my-3">
                            <p>Vous avez déjà un compte? <a href="#">Connectez-vous</a></p>
                            <p><a href="{{path('app_visitor_welcome')}}">Retour à l'accueil</a></p>
                        </div>
                    </div>
                </div>
            </div>
        </main>
        
    </body>

</html>










    


    {# <body class="ma-auth-bg">
        <main>
            <div class="container">
                <div class="row min-vh-100 align-items-center">
                    
                
                    <div class="col-lg-6 d-none d-lg-block text-center">
                        <img
                            class="img-fluid"
                            src="/images/statics/inscription.png"
                            alt="Illustration invitant à l'inscription sur Miftah Arabi"
                        >
                    </div>

                    
                    <div class="col-lg-6 d-flex flex-column gap-3 justify-content-center align-items-center">
                        
                        <h1 class="text-center my-3">Inscription</h1>

                        <div class="w-100" style="max-width: 420px;">
                            <div class="card shadow-sm border-0 rounded-4 p-4">

                                {{ form_start(registrationForm) }}

                                    {{ form_errors(registrationForm) }}

                                    <div class="row">
                                        <div class="col-lg-6 mb-3">
                                            {{ form_label(registrationForm.firstName, 'Prénom') }}
                                            {{ form_widget(registrationForm.firstName, {
                                                attr: {
                                                    class: 'form-control',
                                                    autofocus: true
                                                }
                                            }) }}
                                            {{ form_errors(registrationForm.firstName) }}
                                        </div>

                                        <div class="col-lg-6 mb-3">
                                            {{ form_label(registrationForm.lastName, 'Nom') }}
                                            {{ form_widget(registrationForm.lastName, {
                                                attr: { class: 'form-control' }
                                            }) }}
                                            {{ form_errors(registrationForm.lastName) }}
                                        </div>
                                    </div>

                                    <div class="mb-3">
                                        {{ form_label(registrationForm.email, 'Adresse email') }}
                                        {{ form_widget(registrationForm.email, {
                                            attr: { class: 'form-control' }
                                        }) }}
                                        {{ form_errors(registrationForm.email) }}
                                    </div>

                                    <div class="mb-3">
                                        {{ form_label(registrationForm.password.first, 'Mot de passe') }}
                                        {{ form_widget(registrationForm.password.first, {
                                            attr: {
                                                class: 'form-control',
                                                placeholder: 'Au moins 12 caractères'
                                            }
                                        }) }}
                                        {{ form_errors(registrationForm.password.first) }}
                                        <small class="text-muted">
                                            <i class="fa-solid fa-circle-exclamation"></i>
                                            Au moins une majuscule, une minuscule, un chiffre et un caractère spécial.
                                        </small>
                                    </div>

                                    <div class="mb-3">
                                        {{ form_label(registrationForm.password.second, 'Confirmation du mot de passe') }}
                                        {{ form_widget(registrationForm.password.second, {
                                            attr: { class: 'form-control' }
                                        }) }}
                                        {{ form_errors(registrationForm.password.second) }}
                                    </div>

                                    <div class="form-check mb-3">
                                        {{ form_widget(registrationForm.agreeTerms, {
                                            attr: { class: 'form-check-input' }
                                        }) }}
                                        {{ form_label(
                                            registrationForm.agreeTerms,
                                            "Lire et accepter les conditions générales d'utilisation",
                                            { label_attr: { class: 'form-check-label' } }
                                        ) }}
                                        {{ form_errors(registrationForm.agreeTerms) }}
                                    </div>

                                    <button type="submit" class="btn btn-primary w-100 shadow">
                                        S'inscrire
                                    </button>

                                {{ form_end(registrationForm) }}

                            </div>
                        </div>

                        <div class="text-center my-3">
                            <p>
                                Vous avez déjà un compte ?
                                <a href="#">Connectez-vous</a>
                            </p>
                            <p>
                                <a href="{{ path('app_visitor_welcome') }}">Retour à l'accueil</a>
                            </p>
                        </div>

                    </div>
                </div>
            </div>
        </main>
    </body> #}
